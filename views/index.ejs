<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("partials/head.ejs") %>
</head>
<body>
    <header>
    <h1>Pokédex</h1> 
    <button><a href="/pokemon/new">Create a new Pokédex entry</a></button>
    </header>
    
    <div class="full-list">
        <ul>
            <% for( let index = 0; index < allPokemon.length; index++ ) { %>
                <li>
                    <a href="/pokemon/<%= index %>">
                        <img src="<%= allPokemon[index].img %>" alt="Picture of <%= allPokemon[index].name %>" width="20px" height="20px">
                        <%= allPokemon[index].name %>
                    </a>

                    <form action="/pokemon/<%= index %>/edit" method="GET">
                        <input type="submit" value="Edit">
                    </form>
                    
                    <form action="/pokemon/<%= index %>?_method=DELETE" method="POST">
                        <input type="submit" value="Delete">
                    </form>
                </li>
                <% } %>
            </ul>
        </div>
    </body>
</html>